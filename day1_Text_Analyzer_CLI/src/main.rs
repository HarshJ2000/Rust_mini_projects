/* DAY 1 -> BUILDING A TEXT ANALYZER CLI THAT COUNTS WORD FREQUENCY */

fn main() {
    let str = String::from(
        "This is the sentence which will be provided as input for counting word frequency!",
    );
}
